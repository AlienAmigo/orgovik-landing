nav.main-nav
  ul.main-nav__list
    each item in MainMenuData
      li.main-nav__item
        a(
          href=`${item.link}`,
          class=`main-nav__link ${(item.name == myPageTitle) ? 'main-nav__link--active ' : ''}`)= item.name
